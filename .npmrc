# layercake@10 still pins peer "typescript ^5", which conflicts with our newer
# TypeScript under strict peer resolution (npm 7+, and Vercel CI). The mismatch
# is a stale peer range, not a real incompatibility, so accept it everywhere
# (local, CI, Vercel) instead of needing --legacy-peer-deps on each install.
legacy-peer-deps=true
